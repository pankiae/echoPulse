[project]
name = "echopulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pankaj", email = "pankajjarial@smardatainc.net" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
echopulse = "echopulse:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
